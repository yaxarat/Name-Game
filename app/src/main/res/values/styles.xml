<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/wtPrimary</item>
        <item name="colorPrimaryDark">@color/wtPrimaryDark</item>
        <item name="colorAccent">@color/wtAccent</item>
    </style>
</resources>
